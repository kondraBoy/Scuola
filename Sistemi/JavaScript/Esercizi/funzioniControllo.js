function controllaNum(num){
	let check;
	if (isNaN(n) || n < 0)
		check=false;
	else
		check=true;
	return check;
} 
        
function controllaStr(str){
	let check;
	if(nome!=null || nome!=""){
		check=false;
	}else
		check=true;
	return check;
}